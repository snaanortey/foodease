/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.pravatar.cc', 'icons8.com', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
